{
  "mcpServers": {
    "pseudolife-memory": {
      "type": "http",
      "url": "${PSEUDOLIFE_MCP_DAEMON_URL:-http://127.0.0.1:8765}/mcp",
      "headers": {
        "Authorization": "Bearer ${PSEUDOLIFE_MCP_TOKEN}"
      }
    }
  }
}
